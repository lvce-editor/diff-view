import type { DiffViewState } from '../DiffViewState/DiffViewState.ts'

export const handleContextMenu = (state: DiffViewState, button: number, clientX: number, clientY: number): DiffViewState => {
  return state
}
